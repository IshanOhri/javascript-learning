document.write("<h1> for loop example </h1>");

// for loop is used when the number of iterations is known in advance.

for(var x=1; x <=10; x++)
{
    document.write("<h3>" + (5*x) + "</h3>");
}

var y = 2
for(;y<=10;y+=2)
{
    document.write("<h3>" + (y) + "</h3>");
}