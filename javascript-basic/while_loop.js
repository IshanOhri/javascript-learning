document.write("<h1> while loop example </h1>");

// While loop is used when the number of iterations is not known but the particular condition will be true for certain number of time.

var x = 1;

while(x<10)
{
    document.write("<h3>" + x + "</h3>");
    x+=2;
}